<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <link href="~/Bootstrap/css/bootstrap.css" rel="stylesheet" />
    <link href="~/CSS/Style.css" rel="stylesheet" />
    <title>@ViewBag.Title</title>
</head>
<body>
    <div class="menu">
        <div class="dropdown">
            <button class="dropbtn">Пользователь</button>
            <div class="dropdown-content">
                @Html.RouteLink("Добавить пользователя", new { controller = "User", action = "Index" })
                @Html.RouteLink("Таблица пользователей", new { controller = "User", action = "Show_Table" })
            </div>
        </div>
        <div class="dropdown">
            <button class="dropbtn">Роль</button>
            <div class="dropdown-content">
                @Html.RouteLink("Добавить роль", new { controller = "Role", action = "Index" })
                @Html.RouteLink("Таблица ролей", new { controller = "Role", action = "Show_Table" })
            </div>
        </div>
        <div class="dropdown">
            <button class="dropbtn">Книга</button>
            <div class="dropdown-content">
                @Html.RouteLink("Добавить книгу", new { controller = "Book", action = "Index" })
                @Html.RouteLink("Таблица книг", new { controller = "Book", action = "Show_Table" })
            </div>
        </div>
        <div class="dropdown">
            <button class="dropbtn">Автор</button>
            <div class="dropdown-content">
                @Html.RouteLink("Добавить автора", new { controller = "Author", action = "Index" })
                @Html.RouteLink("Таблица авторов", new { controller = "Author", action = "Show_Table" })
            </div>
        </div>
        <div class="dropdown">
            <button class="dropbtn">Публикация</button>
            <div class="dropdown-content">
                @Html.RouteLink("Добавить публикацию", new { controller = "Publisher", action = "Index" })
                @Html.RouteLink("Таблица публикаций", new { controller = "Publisher", action = "Show_Table" })
            </div>
        </div>
    </div>
<div>
    @RenderBody()
</div>
<script src="~/Bootstrap/js/bootstrap.js"></script>
</body>
</html>
